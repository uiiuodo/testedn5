# 사람 기록 앱 작업지시서 (Flutter + 안티그래비티)

## 1. 프로젝트 개요

- 이름(가칭): **너기 – 사람 기록 앱**
- 목적:

  - 소중한 사람들의 **기념일, 취향, 메모, 일정**을 한곳에 기록하고
    관계를 더 잘 챙길 수 있게 돕는 앱.

- 타깃:

  - 가족, 연인, 절친 등 “소중한 지인” 위주로 기억을 잘 챙기고 싶은 사람.

- 주요 특징:

  - 사람별 카드와 캘린더 중심.
  - 취향/메모/기념일을 한 화면에서 확인 가능.
  - 그룹(가족/지인/직장 등)으로 사람을 분류해서 관리.

---

## 2. 기술 스택

- **Flutter** (최신 안정 버전)
- 상태관리: `Riverpod` 또는 `Provider` 중 하나 사용 (선택 가능, 이하에서는 “상태관리”로 통칭)
- 로컬 저장:

  - **Hive** 기반 로컬 DB (Box 분리: people, groups, schedules, settings)

- 날짜/시간:

  - `intl`
  - 캘린더 UI는 `table_calendar` 등 월간 캘린더 패키지 사용

- 알림:

  - UI만 구현, 실제 푸시 알림/로컬 알림 기능은 이번 버전에서 **구현하지 않음**

- 로그인:

  - Google/Apple **SNS 로그인 버튼 UI만 구현** (실제 인증/백엔드는 이번 버전 범위 아님)

---

## 3. 폴더 구조

```text
lib/
  main.dart
  app/
    app.dart           // MaterialApp, 라우트 설정
    router.dart        // 화면 라우트 정의
    theme.dart         // 공통 색상, 폰트, 버튼 스타일
  common/
    models/            // 데이터 모델 (Person, Group, Schedule 등)
    repositories/      // Hive를 사용하는 Repository 계층
    widgets/           // 공통 위젯 (기본 버튼, 카드, 텍스트필드 등)
    utils/             // 날짜/나이/D-day 계산 등 헬퍼
  feature/
    auth/              // 로그인 화면
    people/            // 사람 목록/상세/수정
    calendar/          // 캘린더 (개인/전체/그룹)
    notification/      // 알림 센터
    mypage/            // 마이페이지 + 나에 대한 기록
```

> 안티그래비티에서 파일 생성 시, 위 구조를 기준으로 폴더와 파일을 만들어 달라고 지시.

---

## 4. 데이터 모델 정의 (Dart 클래스 형태)

### 4.1 Person

```dart
class Person {
  final String id;              // uuid
  final String name;            // 이름
  final String? nickname;       // 애칭 (선택)
  final DateTime? birthDate;    // 생년월일 (선택, 양력 기준)
  final String? phone;
  final String? address;
  final String? email;

  final String groupId;         // 소속 그룹 id (가족/지인/직장 등)

  final List<Anniversary> anniversaries;
  final List<Memo> memos;
  final List<PreferenceCategory> preferences;

  Person(...);
}
```

### 4.2 Group

```dart
class Group {
  final String id;          // uuid
  final String name;        // 예: 가족, 지인, 직장
  final int colorValue;     // Color 값 (파스텔톤)

  Group(...);
}
```

### 4.3 Anniversary (기념일)

```dart
enum AnniversaryType { birthday, etc }

class Anniversary {
  final String id;
  final String personId;
  final String title;            // 생일, 백일, 출산 등
  final DateTime date;
  final AnniversaryType type;

  Anniversary(...);
}
```

### 4.4 Memo (메모)

```dart
class Memo {
  final String id;
  final String personId;
  final DateTime createdAt;
  final String content;

  Memo(...);
}
```

### 4.5 PreferenceCategory (취향 카테고리)

> 카테고리는 **사용자가 직접 생성**하는 형태. (음식/장소/차/사람 등 자유롭게.)

```dart
class PreferenceCategory {
  final String id;
  final String personId;
  final String title;       // 카테고리명 (예: 음식, 장소, 차, 사람)
  final String? like;       // 선호 내용
  final String? dislike;    // 비선호 내용

  PreferenceCategory(...);
}
```

### 4.6 Schedule (일정)

```dart
enum ScheduleType { anniversary, care, etc } // 기념일, 챙기기, 기타

class Schedule {
  final String id;
  final String title;
  final DateTime startDateTime;
  final DateTime endDateTime;
  final bool allDay;

  final ScheduleType type;
  final List<String> personIds;  // 관련된 사람들 (한 명 이상)
  final String? groupId;         // 그룹 일정인 경우

  Schedule(...);
}
```

### 4.7 User (나에 대한 기록)

```dart
class UserProfile {
  final String id;
  final String name;
  final DateTime? birthDate;
  final List<Anniversary> anniversaries;
  final List<Memo> memos;
  final List<PreferenceCategory> preferences;

  UserProfile(...);
}
```

---

## 5. 라우트 구조

`app/router.dart` 예시:

- `/splash` – 스플래시 / 온보딩 카피 화면
- `/login` – 로그인 화면
- `/home` – 메인 탭(홈/캘린더/마이페이지)
- `/people/detail/:personId`
- `/people/edit/:personId?` (없으면 신규)
- `/calendar/person/:personId`
- `/calendar/group`
- `/notification`
- `/mypage`
- `/mypage/me`

---

## 6. 기능별 작업 지시

아래부터는 **화면 단위로 파일/기능/UI 요구사항**을 정리.
안티그래비티가 그대로 구현할 수 있게 구체적으로 기술.

---

### 6.1 공통: 테마 & 기본 위젯

**파일**

- `app/theme.dart`
- `common/widgets/primary_button.dart`
- `common/widgets/section_card.dart` (메모/기념일/취향 카드)

**요구사항**

1. 컬러 팔레트

   - 배경: `#FFFFFF`
   - 텍스트 기본: 진한 그레이 (`#222222` 근처)
   - 서브 텍스트: 중간 그레이
   - 라인/구분선: 연한 그레이
   - 포인트 컬러: 파스텔톤 여러 색 (가족/지인/직장 그룹 컬러로 사용)

2. 폰트

   - Noto Sans 계열
   - 제목, 본문, 캡션 정도로만 크기 나눔.

3. 공통 버튼 (`PrimaryButton`)

   - 둥근 모서리 pill 형태
   - 높이 고정, 가로는 가능한 한 꽉 차게
   - 비활성 상태도 스타일 정의(회색 배경).

---

### 6.2 스플래시 & 온보딩 화면

**파일**

- `feature/auth/splash_screen.dart`

**기능**

- 앱 실행 시 가장 먼저 노출.

- 중앙에 카피 텍스트:

  - 예시

    - “기억해 두면 더 가까워지는 관계가 있습니다”
    - “관계를 돕는 앱, 어쩌구 저쩌구…”

- 2~3초 후 자동으로 `/login`으로 이동.

---

### 6.3 로그인 화면 (UI만, 기능 없음)

**파일**

- `feature/auth/login_screen.dart`

**기능 / UI**

- 제목: “기록을 위해 로그인하기”
- 입력 필드 2개:

  - 이메일
  - 비밀번호

- 중앙에 로그인 버튼

  - 버튼 클릭 시 실제 인증 없이 바로 `/home`으로 이동.

- 하단 텍스트:

  - 회원가입 / 우리 앱 알아보기 (실제 링크 없이 UI만, 또는 단순 다이얼로그)

- SNS 로그인 영역:

  - 동그라미 3개 아이콘 형태 (Google, Apple, 기타)
  - 클릭해도 현재는 아무 기능 없음 (토스트 등으로 “준비 중” 표시 정도만 가능)

---

### 6.4 홈 탭 – 사람 목록 화면

**파일**

- `feature/people/people_list_screen.dart`

**목적**

- 등록된 사람들을 그룹/검색으로 빠르게 찾는 메인 화면.

**UI 구조**

- 상단:

  - 현재 시간 텍스트 (또는 단순 로고/앱 이름)
  - 오른쪽 상단 벨 아이콘 → `/notification`으로 이동

- 필터 영역:

  - `전체` / `선택` 토글 버튼
  - 그룹 칩 리스트 (가족 💙 / 지인 💛 / 직장 💜 / 기타 등)

    - 그룹 색상은 파스텔 원형 점으로 표시.

- 검색창:

  - placeholder: “검색”

- 사람 목록:

  - 각 행:

    - 왼쪽에 컬러 점 (그룹 색)
    - 이름 텍스트
    - 오른쪽에 정렬/드래그 핸들 아이콘

- Floating Action Button:

  - 오른쪽 아래 큰 `+` 버튼
  - 탭 시 `/people/edit` (신규 등록 화면)으로 이동.

**상태/동작**

- 상단 필터에 따라 보여주는 사람 리스트가 변경.
- 검색어 입력 시 이름에 기준해 필터링.
- 그룹 필터는 `GroupRepository`에서 가져온 그룹 목록을 사용.

---

### 6.5 그룹 선택/추가 UI

**파일**

- `feature/people/group_selector.dart`
- `feature/people/group_add_sheet.dart`

**기능**

- 사람 목록 상단에서 그룹 필터 클릭 시, 드롭다운 또는 작은 팝업 메뉴 표시:

  - 기존 그룹 목록 나열 (가족, 지인, 직장 등)
  - 마지막에 “그룹 추가하기” 항목.

- 그룹 추가 화면/바텀시트:

  - 입력: 그룹 이름
  - 컬러 선택: 파스텔 동그라미 컬러 리스트
  - “등록하기” 버튼
  - 저장 후 그룹 목록에 추가 및 현재 선택 그룹으로 설정.

---

### 6.6 사람 추가/수정 화면

**파일**

- `feature/people/person_edit_screen.dart`

**기능**

- 사람의 모든 정보를 입력하거나 수정하는 화면.
- `personId`가 없으면 신규, 있으면 수정 모드.

**UI**

- 상단:

  - 뒤로가기 아이콘
  - 우측 상단: “그룹 설정” 버튼/텍스트 (탭 시 그룹 선택 팝업)

- 입력 필드:

  1. 이름 / 애칭 (필수) – 한 줄 텍스트
  2. 생년월일 – 선택 (DatePicker)
  3. 전화번호 – 선택
  4. 주소 – 선택
  5. 이메일 – 선택

- 기념일 섹션:

  - 제목: “기념일”
  - 리스트 형식, 각 항목은 “타이틀 + 날짜”
  - “+ 기념일 추가” 버튼:

    - 타이틀 입력(예: 생일, 백일)
    - 날짜 선택
    - 저장 시 `Anniversary` 리스트에 추가

- 메모 섹션:

  - 제목: “메모”
  - 큰 텍스트 입력 필드
  - “+ 메모 추가” 버튼: 입력값을 리스트에 추가

- 취향 기록 섹션:

  - 제목: “취향 기록”
  - 기본 상태: 접힌 아코디언
  - 펼치면:

    - “카테고리 제목 입력” 필드 (예: 음식, 장소, 차, 사람 등 자유 입력)
    - 라디오 버튼: 선호 / 비선호 선택
    - 내용 텍스트 필드
    - “추가하기” 버튼 → `PreferenceCategory` 생성

  - 여러 카테고리 등록 가능.

- 하단:

  - “등록하기” 또는 “수정하기” 버튼
  - 버튼 클릭 시 `PersonRepository`를 통해 Hive에 저장 후 이전 화면으로 복귀.

---

### 6.7 사람 상세 화면

**파일**

- `feature/people/person_detail_screen.dart`

**기능**

- 한 사람에 대한 정보를 한눈에 보는 화면.

**UI 구조**

- 상단:

  - 뒤로가기 아이콘
  - 우측 상단 연필 아이콘 → `/people/edit/:personId`로 이동

- 상단 정보 블록:

  - 이름 (큰 타이틀)
  - 생년월일 + 나이 (예: “1996.10.29 (만 29세)”)
  - 전화번호, 주소, 이메일 한 줄씩 표시

- 기념일 블록:

  - 카드 리스트 형태
  - 예:

    - “생일 – 25년 10월 10일 → 음력 생일”
    - “백일 – 26년 2월 14일 첫째 딸 지영이 백일”

- 메모 블록:

  - 여러 개의 메모 카드, 최신 순 정렬
  - 긴 텍스트도 박스 안에서 줄바꿈 되어 표시

- 취향 기록 블록:

  - 카테고리별로 섹션
  - 각 카테고리 카드 안에:

    - “선호” 박스
    - “비선호” 박스

  - 필요하면 접기/펼치기 형태.

- 하단:

  - “개인 캘린더로 이동하기” 버튼 → `/calendar/person/:personId`

---

### 6.8 개인 캘린더 화면

**파일**

- `feature/calendar/person_calendar_screen.dart`

**기능**

- 특정 사람과 관련된 일정들을 월간 캘린더로 보여주고, 일정 추가/수정이 가능한 화면.

**UI**

- 상단:

  - 뒤로가기 아이콘
  - 우측 상단 연필 아이콘 (추후 개인 캘린더 설정 등 확장 가능, 현재는 기능 없음 또는 일정 전체 편집 진입)

- 중간:

  - 월간 캘린더 (현재 선택 월)
  - 날짜에 일정이 있는 경우 박스/점으로 표시

    - 예: “데이트”, “저녁식사” 텍스트가 작은 폰트로 들어간 타일

- 하단:

  - 큰 버튼: “+ 이 사람과의 일정 추가하기”
  - 아래에 “계획해야 하는 일정” 리스트:

    - 예:

      - “26년 1월 신년맞이 등산계획 (미정) > 이도, 인선, 경하”
      - “26년 5월 10일 오후 8시 이도가 좋아하는 공연 예매하기”

**동작**

- 날짜 탭 시 해당 날짜의 일정 리스트를 보여주면서 일정 상세 팝업.
- “+ 이 사람과의 일정 추가하기” 탭 시 일정 생성 화면으로 이동.

---

### 6.9 일정 추가/수정 화면

**파일**

- `feature/calendar/schedule_edit_screen.dart`

**기능**

- 새로운 일정을 생성하거나 수정.

**UI**

- 상단:

  - 뒤로가기
  - 제목: “일정”

- 본문:

  - 텍스트 입력: “일정을 입력하세요”
  - 토글: “종일”
  - 시작 날짜/시간 선택
  - 종료 날짜/시간 선택
  - “반복” 설정 버튼 (이번 버전에서는 단순 토글/옵션 UI만, 실제 로직은 최소 구현 또는 미구현 가능)
  - “알림” 설정 버튼 (UI만, 실제 알림은 미구현)
  - “기념일” 토글
  - “챙기기” 토글

- 하단:

  - “추가하기” / “수정하기” 버튼 → `ScheduleRepository`를 통해 Hive에 저장

---

### 6.10 전체/그룹 캘린더 화면

**파일**

- `feature/calendar/group_calendar_screen.dart`

**기능**

- 여러 사람과 그룹 기준으로 전체 일정을 모아 보는 화면.

**UI**

- 상단:

  - 뒤로가기
  - 제목 부분에 연도/월 표시 (예: “2025 11”)

- 필터 영역:

  - 칩: `전체`, `선택`
  - 그룹 필터 칩들 (가족/지인/직장/나얼배움캠프 등)

- 중간:

  - 월간 캘린더
  - 날짜마다 하나 이상 일정이 있으면 박스로 표시
  - 일정 내용 요약 텍스트 (예: “엄마와 등산 예정”)

- 하단:

  - “+ 그룹 일정 추가하기” 버튼
  - 아래 “계획해야 하는 일정” 리스트:

    - 예:

      - “12월 14, 20일 중 파티 날 정하기”
      - “공연장 근처 카페 찾기” 등

---

### 6.11 알림 센터 화면

**파일**

- `feature/notification/notification_center_screen.dart`

**기능**

- 다가오는 기념일/챙기기 일정들을 리스트로 보여주는 화면.
- 실제 **푸시 알림은 구현하지 않고**, 단순 D-day 리스트만 제공.

**UI**

- 상단:

  - “알림 센터” 제목
  - 필터 탭: `전체` / `기념일` / `챙기기`

- 섹션 1 – 기념일:

  - “D-5 안은영 생일”
  - “D-21 김시선 생일”
  - “D-74 인선 딸 지영 백일” 등.

- 섹션 2 – 챙기기:

  - “D-day 팀 막내 입사 1주년 축하”
  - “D-2 할머니께 안부 전화드리기”

**동작**

- 각 항목을 탭하면:

  - 관련된 사람 상세 혹은 캘린더 화면으로 이동 (MVP에서는 사람 상세로만 이동해도 됨).

---

### 6.12 마이페이지 & 나에 대한 기록

#### 6.12.1 마이페이지 메인

**파일**

- `feature/mypage/mypage_screen.dart`

**UI**

- 섹션: “나에 대한 기록” (맨 위 큰 버튼) → `/mypage/me`
- 계정 설정: “로그인 정보” (UI만)
- 서비스 설정:

  - 알림 설정 (UI만)
  - 구독/결제 관리 (UI만)
  - 백업 (UI만, “준비 중” 다이얼로그)
  - 언어 설정 (UI만)

- 고객지원:

  - FAQ
  - 1:1 문의 (UI만)

- 하단: 로그아웃 텍스트 버튼 (현 버전에서 실제 세션 없음 → 단순 `/login` 으로 이동 정도)

#### 6.12.2 나에 대한 기록 화면

**파일**

- `feature/mypage/my_profile_screen.dart`

**기능**

- 자기 자신을 하나의 Person처럼 기록할 수 있는 화면.

**UI**

- 상단:

  - 뒤로가기
  - 제목: “나에 대한 기록”

- 상단 정보:

  - 생년월일 + 나이

- 기념일:

  - 내 생일, 기념일 등

- 메모:

  - 내 성향, 말 습관, 감정 패턴 등에 대한 메모

- 취향 기록:

  - 책/영화/음식 등 카테고리별 선호/비선호

구조는 `person_detail_screen`과 거의 동일하게 구현.

---

## 7. 개발 순서 (마일스톤)

안티그래비티가 구현할 때 참고할 수 있는 **우선순위** 정리.

### 1차 마일스톤 (MVP UI + 로컬 저장)

1. 프로젝트 세팅 + 테마
2. 스플래시 / 로그인 (UI만)
3. 홈 탭 – 사람 목록 화면
4. 그룹 추가/필터 UI
5. 사람 추가/수정 화면
6. 사람 상세 화면
7. Hive Repository 구현 (Person, Group)

### 2차 마일스톤 (캘린더 & 일정)

1. 개인 캘린더 화면
2. 일정 추가/수정 화면
3. 전체/그룹 캘린더 화면
4. Schedule 데이터 모델 + Repository

### 3차 마일스톤 (알림 센터 & 마이페이지)

1. 알림 센터 화면 (D-day 계산)
2. 마이페이지 메인
3. 나에 대한 기록 화면

> 각 마일스톤마다, 우선 **UI 스켈레톤 → 더미 데이터 연결 → 실제 Hive 데이터 연결** 순서로 진행.
